function add-time
	
    # set added $argv[2]
    # set hourst (echo $time | cut -d ":" -f1)
    # set minutest (echo $time | cut -d ":" -f2)
    # set hoursa (echo $added | cut -d ":" -f1)
    # set minutesa (echo $added | cut -d ":" -f2)
    # set finalhours (wcalc -q "$hourst + $hoursa")
    # set finalminutes (wcalc -q "$minutest + $minutesa")
    # set $finalhours (math "$finalhours + $finalminutes / 60")
    # set finalminutes (math "finalminutes % 60")

    # echo ht $hourst mt $minutest ha $hoursa ma $minutesa
    # echo fh $finalhours fm $finalminutes

end
